{% extends 'base.html.twig' %}

{% block title %}Calculated Bills{% endblock %}

{% block body %}
    <div class="table-container">
        <h1>Calculated Bills</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Household</th>
                    <th>Total Cost (GBP)</th>
                </tr>
            </thead>
            <tbody>
                {% for result in results %}
                    <tr>
                        <td>{{ result.house.postcode }}</td>
                        <td>£{{ result.totalCost }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}